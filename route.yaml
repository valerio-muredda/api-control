# This Route exposes the Service to the outside world via a public URL.
# -----------------------------------------------------------------------------
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: api-stress-tester-route
  namespace: default
spec:
  to:
    kind: Service
    name: api-stress-tester-service
  port:
    targetPort: 8080
